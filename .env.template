# ============================================================
# qiqibot 环境变量配置模板
# 复制为 .env.dev（开发）或 .env.prod（生产）后填写
# ============================================================

# ── NoneBot2 核心配置 ──────────────────────────────────────

# 驱动器，fastapi 提供 HTTP 服务，websockets 提供 WS 客户端
DRIVER=~fastapi+~websockets

# 监听地址和端口
HOST=0.0.0.0
PORT=8081

# 日志级别：DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=DEBUG

# 超级用户 QQ 号列表
SUPERUSERS=["你的QQ号"]

# 命令前缀（已改用自然语言触发，保留即可）
COMMAND_START=["/", ""]
COMMAND_SEP=["."]

# ── OneBot v11 配置（NapCatQQ / go-cqhttp）────────────────

# WebSocket 反向连接地址（NoneBot 主动连 OneBot）
ONEBOT_WS_URLS=["ws://127.0.0.1:3001/onebot/v11/ws"]

# OneBot 访问令牌（需与 OneBot 端一致）
ONEBOT_ACCESS_TOKEN=

# API 调用超时（秒）
# ONEBOT_API_TIMEOUT=60

# ── 视频下载插件 ────────────────────────────────────────────

# 下载代理（留空则不使用代理）
VIDEO_PROXY=

# 从哪个浏览器读取 cookies（用于需要登录的平台）
# 可选：chrome / firefox / edge / safari / 留空禁用
VIDEO_COOKIES_BROWSER=chrome

# 视频临时保存目录（下载后自动清理）
VIDEO_SAVE_DIR=downloads

# ── 问答知识库插件 ──────────────────────────────────────────

# 列表/搜索结果最大条数
QA_LIST_LIMIT=10

# 每条问答最多附带图片数
QA_MAX_IMAGES=5

# ── 示例插件 ────────────────────────────────────────────────

# 是否启用示例插件
DEMO_ENABLED=true

# 是否启用复读功能
DEMO_ECHO=true
